/// whisper-rs context and state management.
///
/// Wraps `WhisperContext` and `WhisperState` from whisper-rs,
/// providing a higher-level API for model lifecycle and inference.
///
/// The actual whisper-rs integration is commented out during
/// infrastructure phase — activation happens during feature implementation.
pub struct InferenceEngine {
    // model_path: std::path::PathBuf,
    // context: Option<whisper_rs::WhisperContext>,
}
