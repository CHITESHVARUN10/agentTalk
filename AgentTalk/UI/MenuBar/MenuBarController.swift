import AppKit

/// Manages the menu bar icon and contextual menu.
///
/// Future responsibilities:
/// - Status indicator (idle, recording, processing)
/// - Quick actions (start/stop recording, settings, quit)
final class MenuBarController {
    // Placeholder — wired during feature implementation
}
